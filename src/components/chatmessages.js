import dataset from "../data/dataset";

export const messages  = (movie, message1, message2) =>{

    const messagesContainer = document.createElement("section");
    const movieChat = data.finc((item) => item.name==movie.name);
    messagesContainer.setAttribute("class", "messageContainer");
    messagesContainer.innerHTML =`
    <div>
    </div>








    `
}